all:
  hosts:
    master.local:
    build.local:
    worker-1.local:
  children:
    build:
      hosts:
        build.local:
    master:
      hosts:
        master.local:
    worker:
      hosts:
        worker-1.local:
    kuberenetes:
      hosts:
        master.local:
        worker-1.local:
